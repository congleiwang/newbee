<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="jdbc.properties">
        <property name="org.apache.ibatis.parsing.PropertyParser.enable-default-value" value="true"/>
        <property name="logPrefix" value="mybatis"/>
        <property name="logImpl" value="SLF4J"/>
    </properties>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>


    <typeAliases>
        <package name="wang.conglei.mybatis.entity"/>
    </typeAliases>

    <typeHandlers>
        <typeHandler handler="wang.conglei.mybatis.handler.BooleanTypeHandler"/>
    </typeHandlers>

    <objectFactory type="wang.conglei.mybatis.factory.MyObjectFactory"/>

    <plugins>
        <plugin interceptor="wang.conglei.mybatis.plugin.ExcutorIntercepter"></plugin>
    </plugins>

    <environments default="dev">
        <environment id="dev">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <package name="wang.conglei.mybatis.dao"/>
        <!--
                <mapper resource="mapper/deptMapper.xml" />
        -->
    </mappers>


</configuration>